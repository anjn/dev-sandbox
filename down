#!/usr/bin/env bash

set -euo pipefail

source "$(dirname -- "$(readlink -f -- "$0")")/lib/sandbox.sh"

platform=$(sandbox_resolve_platform)

sandbox_require_commands podman podman-compose

sandbox_compose "$platform" down

podman ps -a
